---

name: Kairos Extensions Pipeline

on:
  push:
    branches:
      - main
    paths:
      - kairos-extensions/**
      - .github/workflows/kairos-extensions.yml


jobs:
  # matrix:
  #   runs-on: ubuntu-latest
  #   outputs:
  #     matrix: ${{ steps.matrix.outputs.matrix }}
  #   steps:
  #     - uses: actions/checkout@v3
  #     - name: Enumerate extensions & load version matrix
  #       id: matrix
  #       run: |
  #         MATRIX=$(find kairos-extensions/ -iname 'Dockerfile' | jq --slurp --raw-input 'split("\n")[:-1]' | jq '
  #           {
  #             "include": (
  #               map(
  #                 {
  #                   "extension": (split("/")[1]),
                    
  #                 }
  #               )
  #             )
  #           }
  #         ')
  #         echo ::set-output name=matrix::${MATRIX}

  build:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        extension:
          - tailscale
        kairos-version:
          - v1.3.2
          - v1.4.0
    steps:
      - uses: actions/checkout@v4
      - run: |
          git fetch --prune --unshallow
      - name: setup-docker
        uses: docker-practice/actions-setup-docker@master
      - uses: earthly/actions-setup@v1
        with:
          version: "latest"
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
          logout: false
      - run: earthly --ci --push +kairos-extension-all-platforms --EXTENSION=${{ matrix.extension }} --KAIROS_VERSION=${{ matrix.kairos-version }}
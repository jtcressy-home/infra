apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: newrelic
spec:
  refreshInterval: 1m
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: newrelic
    creationPolicy: Owner
  dataFrom:
    - extract:
        conversionStrategy: Default
        decodingStrategy: None
        key: newrelic-core-cluster
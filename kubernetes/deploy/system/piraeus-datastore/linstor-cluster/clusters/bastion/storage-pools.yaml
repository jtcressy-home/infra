apiVersion: piraeus.io/v1
kind: LinstorSatelliteConfiguration
metadata:
  name: home-office-rack-default-pool
spec:
  nodeSelector:
    topology.kubernetes.io/zone: home-office-rack
  storagePools:
    - name: home-office-rack-default
      filePool: 
        directory: /var/lib/linstor-pools/home-office-rack-default
# ---
# apiVersion: piraeus.io/v1
# kind: LinstorSatelliteConfiguration
# metadata:
#   name: oldbessie-zfs
# spec:
#   nodeSelector:
#     kubernets.io/hostname: oldbessie
#   storagePools:
#     - name: oldbessie-zfs
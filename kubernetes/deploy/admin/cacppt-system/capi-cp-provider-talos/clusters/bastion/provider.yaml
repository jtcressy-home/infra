apiVersion: operator.cluster.x-k8s.io/v1alpha2
kind: ControlPlaneProvider
metadata:
 name: talos
 namespace: cacppt-system
spec:
  version: v0.5.4
  # configSecret:
  #   name: talos-variables
  fetchConfig:
    url: https://github.com/siderolabs/cluster-api-control-plane-provider-talos/releases
---
apiVersion: v1
kind: Service
metadata:
  name: metamcp
  labels:
    app.kubernetes.io/name: metamcp
spec:
  selector:
    app.kubernetes.io/name: metamcp
  ports:
    - port: 12008
      targetPort: 12008
      name: http
  type: ClusterIP

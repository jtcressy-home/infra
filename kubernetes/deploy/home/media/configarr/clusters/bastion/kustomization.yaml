apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - cronjob.yaml
  - externalsecret.yaml
  - persistence.yaml

configMapGenerator:
  - name: configarr
    files:
      - config.yml=./resources/config.yml
generatorOptions:
  disableNameSuffixHash: true

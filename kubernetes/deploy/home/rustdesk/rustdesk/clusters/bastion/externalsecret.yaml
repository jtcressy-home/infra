apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: rustdesk-secrets
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: rustdesk-secrets
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: rustdesk-env

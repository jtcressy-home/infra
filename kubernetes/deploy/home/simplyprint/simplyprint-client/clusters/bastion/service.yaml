apiVersion: v1
kind: Service
metadata:
  name: simplyprint-client
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: simplyprint-client
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
